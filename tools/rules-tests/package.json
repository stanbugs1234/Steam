{
  "name": "steam-rules-tests",
  "private": true,
  "type": "module",
  "description": "Security-rules regression tests for firestore.rules and storage.rules, run against the local emulators.",
  "scripts": {
    "test": "cd ../.. && firebase emulators:exec --project demo-steam-rules --only firestore,storage \"node tools/rules-tests/rules.test.mjs\""
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
